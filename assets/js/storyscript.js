$('#myRightArrow').click(function() {
    var currentSlide = $('.showFade.active');
    var nextSlide = currentSlide.next();

    currentSlide.fadeOut(0).removeClass('active');
    nextSlide.fadeIn(200).addClass('active');

    if (nextSlide.length == 0) {
        $('.showFade').first().fadeIn(300).addClass('active');
    }
});

$('#myRightArroww').click(function() {
    var currentSlide = $('.showFade.active');
    var nextSlide = currentSlide.next();

    currentSlide.fadeOut(0).removeClass('active');
    nextSlide.fadeIn(200).addClass('active');

    if (nextSlide.length == 0) {
        $('.showFade').first().fadeIn(300).addClass('active');
    }
});

$('#myRightArrowww').click(function() {
    var currentSlide = $('.showFade.active');
    var nextSlide = currentSlide.next();

    currentSlide.fadeOut(0).removeClass('active');
    nextSlide.fadeIn(200).addClass('active');

    if (nextSlide.length == 0) {
        $('.showFade').first().fadeIn(300).addClass('active');
    }
});

$('#myRightArrowwww').click(function() {
    var currentSlide = $('.showFade.active');
    var nextSlide = currentSlide.next();

    currentSlide.fadeOut(0).removeClass('active');
    nextSlide.fadeIn(200).addClass('active');

    if (nextSlide.length == 0) {
        $('.showFade').first().fadeIn(300).addClass('active');
    }
});

$('#myRightArrowwwww').click(function() {
    var currentSlide = $('.showFade.active');
    var nextSlide = currentSlide.next();

    currentSlide.fadeOut(0).removeClass('active');
    nextSlide.fadeIn(200).addClass('active');

    if (nextSlide.length == 0) {
        $('.showFade').first().fadeIn(300).addClass('active');
    }
});

$('#myRightArrowwwwww').click(function() {
    var currentSlide = $('.showFade.active');
    var nextSlide = currentSlide.next();

    currentSlide.fadeOut(0).removeClass('active');
    nextSlide.fadeIn(200).addClass('active');

    if (nextSlide.length == 0) {
        $('.showFade').first().fadeIn(300).addClass('active');
    }
});

$('#myRightArrowwwwwww').click(function() {
    var currentSlide = $('.showFade.active');
    var nextSlide = currentSlide.next();

    currentSlide.fadeOut(0).removeClass('active');
    nextSlide.fadeIn(200).addClass('active');

    if (nextSlide.length == 0) {
        $('.showFade').first().fadeIn(300).addClass('active');
    }
});

$('#myRightArrow2').click(function() {
    var currentSlide = $('.showFade.active');
    var nextSlide = currentSlide.next();

    currentSlide.fadeOut(0).removeClass('active');
    nextSlide.fadeIn(200).addClass('active');

    if (nextSlide.length == 0) {
        $('.showFade').first().fadeIn(300).addClass('active');
    }
});

$('#myRightArrow3').click(function() {
    var currentSlide = $('.showFade.active');
    var nextSlide = currentSlide.next();

    currentSlide.fadeOut(0).removeClass('active');
    nextSlide.fadeIn(200).addClass('active');

    if (nextSlide.length == 0) {
        $('.showFade').first().fadeIn(300).addClass('active');
    }
});

$('#myRightArrow4').click(function() {
    var currentSlide = $('.showFade.active');
    var nextSlide = currentSlide.next();

    currentSlide.fadeOut(0).removeClass('active');
    nextSlide.fadeIn(200).addClass('active');

    if (nextSlide.length == 0) {
        $('.showFade').first().fadeIn(300).addClass('active');
    }
});

$('#myLeftArrow').click(function () {
    var currentSlide = $('.showFade.active');
    var backSlide = currentSlide.prev();

    currentSlide.fadeOut(0).removeClass('active');
    backSlide.fadeIn(200).addClass('active');

    if (backSlide.length == 0) {
        $('.showFade').last().fadeIn(300).addClass('active');
    }
});
$('#myLeftArroww').click(function () {
    var currentSlide = $('.showFade.active');
    var backSlide = currentSlide.prev();

    currentSlide.fadeOut(0).removeClass('active');
    backSlide.fadeIn(200).addClass('active');

    if (backSlide.length == 0) {
        $('.showFade').last().fadeIn(300).addClass('active');
    }
});